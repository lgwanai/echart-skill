---
phase: 02-performance-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database.py
  - scripts/data_importer.py
  - scripts/chart_generator.py
  - scripts/data_exporter.py
  - scripts/data_cleaner.py
  - tests/test_database.py
autonomous: true
requirements:
  - PERF-01
  - PERF-05
must_haves:
  truths:
    - "User can import data without database locking errors"
    - "User can generate charts while another process imports data"
    - "Database operations complete faster due to connection reuse"
    - "System handles multiple concurrent database users gracefully"
  artifacts:
    - path: "database.py"
      provides: "DatabaseRepository class with connection pooling"
      exports: ["DatabaseRepository", "get_repository"]
      contains: "class DatabaseRepository"
    - path: "tests/test_database.py"
      provides: "Unit tests for connection pool and WAL mode"
      exports: ["test_connection_pool", "test_wal_mode"]
  key_links:
    - from: "DatabaseRepository"
      to: "sqlite3.connect()"
      via: "Queue-based pool"
      pattern: "Queue\\(maxsize=pool_size\\)"
    - from: "scripts/data_importer.py"
      to: "DatabaseRepository"
      via: "get_repository(db_path)"
      pattern: "get_repository"
---

<objective>
Create DatabaseRepository class with connection pooling and WAL mode to replace scattered sqlite3.connect() calls throughout the codebase.

Purpose: Enable connection reuse and concurrent read/write access for multi-agent scenarios.
Output: database.py module with tested DatabaseRepository class, integrated into all scripts.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-performance-optimization/02-CONTEXT.md
@.planning/phases/02-performance-optimization/02-RESEARCH.md
@.planning/phases/02-performance-optimization/02-VALIDATION.md

<interfaces>
<!-- Key interfaces from existing codebase that DatabaseRepository will replace -->

From scripts/data_importer.py:
```python
conn = sqlite3.connect(db_path)  # Line 156 - needs replacement
cursor = conn.cursor()
conn.commit()
conn.close()
```

From scripts/chart_generator.py:
```python
conn = sqlite3.connect(db_path)  # Line 240 - needs replacement
df = pd.read_sql_query(query, conn)
conn.close()
```

From scripts/data_exporter.py:
```python
conn = sqlite3.connect(db_path)  # Line 27 - needs replacement
```

From scripts/data_cleaner.py:
```python
conn = sqlite3.connect(db_path)  # Line 20 - needs replacement
```

From tests/conftest.py:
```python
@pytest.fixture
def temp_db():
    conn = sqlite3.connect(db_path)  # Existing test fixture pattern
```

<!-- Configuration from CONTEXT.md decisions -->
- Pool size: 5 connections
- WAL mode: enabled
- Connection timeout: 5 seconds (default)
</interfaces>
</context>

<tasks>

<task type="auto">
<name>Task 0: Create test scaffold for DatabaseRepository</name>
<files>tests/test_database.py</files>
<action>
Create tests/test_database.py with test scaffold (Wave 0 - Nyquist compliance).

Test scaffold with empty test placeholders:
1. `test_connection_pool_size`: Create repository with pool_size=3, verify 3 connections in pool
2. `test_connection_context_manager`: Get connection from pool, use it, verify returned
3. `test_wal_mode_enabled`: Query PRAGMA journal_mode, verify returns 'wal'
4. `test_execute_query`: Insert test data, query with execute_query, verify results
5. `test_execute_many`: Insert 100 rows with execute_many, verify count
6. `test_concurrent_read`: Use threading to read from multiple connections simultaneously

Each test should have:
- Clear docstring describing expected behavior
- pytest.raises or assertion placeholders
- One test that will FAIL until implementation exists

This ensures Wave 0 test existence per Nyquist rule.
</action>
<verify>
<automated>test -f tests/test_database.py && grep -q "def test_connection_pool_size" tests/test_database.py && echo "OK"</automated>
</verify>
<done>test_database.py exists with 6+ test function stubs</done>
</task>

<task type="auto" tdd="true">
<name>Task 1: Implement test cases for DatabaseRepository</name>
<files>tests/test_database.py</files>
<behavior>
- Test 1: DatabaseRepository creates pool with specified size
- Test 2: connection() context manager returns and returns connections
- Test 3: WAL mode is enabled on all connections
- Test 4: execute_query returns list of dicts
- Test 5: execute_many inserts multiple rows efficiently
- Test 6: Concurrent access does not cause locking errors
</behavior>
<action>
Implement test cases in tests/test_database.py (replacing scaffold from Task 0).

Test cases:
1. `test_connection_pool_size`: Create repository with pool_size=3, verify 3 connections in pool
2. `test_connection_context_manager`: Get connection from pool, use it, verify returned
3. `test_wal_mode_enabled`: Query PRAGMA journal_mode, verify returns 'wal'
4. `test_execute_query`: Insert test data, query with execute_query, verify results
5. `test_execute_many`: Insert 100 rows with execute_many, verify count
6. `test_concurrent_read`: Use threading to read from multiple connections simultaneously

Use tempfile for test database. Use existing conftest.py fixtures as reference.
</action>
<verify>
<automated>pytest tests/test_database.py -v</automated>
</verify>
<done>test_database.py exists with 6+ test functions, tests FAIL until implementation</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Implement DatabaseRepository class</name>
<files>database.py</files>
<behavior>
- Creates connection pool with configurable size (default 5)
- Enables WAL mode on each connection
- Provides context manager for safe connection handling
- Provides execute_query for SELECT operations
- Provides execute_many for bulk INSERT/UPDATE
- Implements close_all for cleanup
</behavior>
<action>
Create database.py in project root with DatabaseRepository class implementation.

Follow the pattern from 02-RESEARCH.md Pattern 1:
- Use Queue for connection pool (thread-safe)
- Set check_same_thread=False for pool connections
- Execute PRAGMA journal_mode=WAL on each connection
- Execute PRAGMA synchronous=NORMAL for better WAL performance
- Use @contextmanager for connection() method
- Return dict rows using sqlite3.Row factory

Include module-level singleton via get_repository() function for convenience.

Implementation requirements:
- Type annotations on all methods
- Docstrings on class and public methods
- atexit handler to close pool on program exit
</action>
<verify>
<automated>pytest tests/test_database.py -v</automated>
</verify>
<done>database.py exists, DatabaseRepository class implements all behaviors, all tests pass</done>
</task>

<task type="auto">
<name>Task 3: Wire DatabaseRepository into data_importer.py</name>
<files>scripts/data_importer.py</files>
<action>
Refactor scripts/data_importer.py to use DatabaseRepository instead of direct sqlite3.connect().

Changes:
1. Add import: `from database import get_repository`
2. In import_to_sqlite() (line 156): Replace `conn = sqlite3.connect(db_path)` with `repo = get_repository(db_path)` and use `with repo.connection() as conn:`
3. Update any direct cursor operations to use the context manager pattern

This enables:
- Connection reuse across multiple import calls
- WAL mode for concurrent access
- Proper connection cleanup

Keep the function signature unchanged for backward compatibility.
</action>
<verify>
<automated>pytest tests/test_data_importer.py -v</automated>
</verify>
<done>data_importer.py uses DatabaseRepository, existing tests still pass</done>
</task>

<task type="auto">
<name>Task 4: Wire DatabaseRepository into chart_generator.py</name>
<files>scripts/chart_generator.py</files>
<action>
Refactor scripts/chart_generator.py to use DatabaseRepository instead of direct sqlite3.connect().

Changes:
1. Add import: `from database import get_repository`
2. Around line 240: Replace `conn = sqlite3.connect(db_path)` with `repo = get_repository(db_path)` and use `with repo.connection() as conn:`

This enables concurrent chart generation while data is being imported.
</action>
<verify>
<automated>pytest tests/test_chart_generator.py -v</automated>
</verify>
<done>chart_generator.py uses DatabaseRepository, existing tests still pass</done>
</task>

<task type="auto">
<name>Task 5: Wire DatabaseRepository into data_exporter.py and data_cleaner.py</name>
<files>scripts/data_exporter.py, scripts/data_cleaner.py</files>
<action>
Refactor remaining scripts to use DatabaseRepository:

scripts/data_exporter.py (line 27):
- Replace `conn = sqlite3.connect(db_path)` with `repo = get_repository(db_path)` and context manager

scripts/data_cleaner.py (line 20):
- Replace `conn = sqlite3.connect(db_path)` with `repo = get_repository(db_path)` and context manager

This completes the integration across all core scripts.
</action>
<verify>
<automated>pytest tests/test_data_exporter_full.py tests/test_data_cleaner.py -v</automated>
</verify>
<done>All core scripts use DatabaseRepository, existing tests pass</done>
</task>

<task type="auto">
<name>Task 6: Verify requirements.txt has pytest</name>
<files>requirements.txt</files>
<action>
No new dependencies required for DatabaseRepository (uses stdlib).

Verify that requirements.txt has pytest and pytest-cov for testing.
If missing, add them. The file should already have these from Phase 1.

Just verify the file and ensure no changes are needed.
</action>
<verify>
<automated>grep -q "pytest" requirements.txt && echo "pytest found"</automated>
</verify>
<done>requirements.txt verified to contain pytest and pytest-cov</done>
</task>

</tasks>

<verification>
Run test suite after all tasks:
```bash
pytest tests/test_database.py -v --cov=database --cov-report=term-missing
```

Integration verification:
```bash
pytest tests/test_data_importer.py tests/test_chart_generator.py -v
```

Verify:
- All 6+ database tests pass
- Coverage of database.py is >= 90%
- WAL mode test confirms 'wal' returned from PRAGMA query
- Existing script tests still pass with new repository integration
</verification>

<success_criteria>
- DatabaseRepository class exists in database.py
- Connection pool creates 5 connections by default
- WAL mode enabled on all connections
- Context manager returns connections to pool
- All core scripts (data_importer, chart_generator, data_exporter, data_cleaner) use DatabaseRepository
- All tests pass with >= 90% coverage
</success_criteria>

<output>
After completion, create `.planning/phases/02-performance-optimization/02-01-SUMMARY.md`
</output>
