---
phase: 05-gantt-chart-api
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - SKILL.md
autonomous: true
requirements:
  - CHART-02
user_setup: []

must_haves:
  truths:
    - "User can read Gantt chart API documentation in SKILL.md"
    - "User can see example usage for creating Gantt charts"
    - "Documentation matches actual API behavior"
  artifacts:
    - path: "SKILL.md"
      provides: "User-facing documentation for Gantt chart feature"
      contains: "Scenario 9: Gantt Chart"
  key_links:
    - from: "SKILL.md"
      to: "scripts/gantt_chart.py"
      via: "API documentation"
      pattern: "generate_gantt_chart"
---

<objective>
Add Gantt chart documentation and examples to SKILL.md so users understand how to use the simplified Gantt API.

Purpose: Provide clear, actionable documentation for the Gantt chart feature following existing SKILL.md patterns.
Output: Updated SKILL.md with Scenario 9 for Gantt charts
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-gantt-chart-api/05-RESEARCH.md

<!-- Reference to completed implementation -->
@scripts/gantt_chart.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Scenario 9 (Gantt Chart) to SKILL.md</name>
  <files>SKILL.md</files>
  <action>
    Add "### Scenario 9: Gantt Chart Generation" section to SKILL.md after Scenario 8.

    Follow the existing pattern from other scenarios:
    1. Add trigger condition: "User requests project timeline or task schedule visualization"
    2. Document the action steps using generate_gantt_chart
    3. Provide example code:

    ```markdown
    ### Scenario 9: Gantt Chart Generation
    **Trigger**: User requests project timeline or task schedule visualization.
    **Action**:
    1. Use the built-in Gantt chart wrapper for simplified API:
       ```python
       from scripts.gantt_chart import generate_gantt_chart

       config = {
           "title": "Project Timeline",
           "tasks": [
               {"name": "Design", "start": "2024-01-01", "end": "2024-01-15"},
               {"name": "Development", "start": "2024-01-10", "end": "2024-02-01"},
               {"name": "Testing", "start": "2024-01-25", "end": "2024-02-10"}
           ]
       }
       output_path = generate_gantt_chart(config)
       ```
    2. The generated HTML will open in browser with interactive Gantt chart.
    3. Tasks support optional fields:
       - `category`: For grouping tasks in rows
       - `color`: Custom bar color (hex code)

    **Note**: Dates can be ISO strings or datetime objects. End date must be after start date.
    ```

    Place this after Scenario 8 (Metrics Management) and before any closing sections.

    Ensure the documentation:
    - Matches actual API (GanttTask fields, generate_gantt_chart signature)
    - Shows practical example with clear task structure
    - Mentions date format requirements
    - Notes the validation behavior
  </action>
  <verify>
    <automated>grep -q "Scenario 9.*Gantt" SKILL.md && grep -q "generate_gantt_chart" SKILL.md</automated>
  </verify>
  <done>SKILL.md contains Scenario 9 with Gantt chart documentation and examples</done>
</task>

<task type="auto">
  <name>Task 2: Update Scenario 4 (Chart Generation) to mention Gantt support</name>
  <files>SKILL.md</files>
  <action>
    Update Scenario 4 in SKILL.md to include Gantt charts in the list of supported chart types.

    Find the line mentioning chart types and update it to include Gantt:
    - Current: "bar, pie, line, scatter, map, funnel, 3D charts, etc."
    - Update to: "bar, pie, line, scatter, map, funnel, 3D charts, Gantt, etc."

    Also add a note that Gantt charts use a dedicated API (Scenario 9) rather than the template-based approach.
  </action>
  <verify>
    <automated>grep -q "Gantt" SKILL.md | head -5</automated>
  </verify>
  <done>Scenario 4 mentions Gantt chart support and references Scenario 9</done>
</task>

</tasks>

<verification>
- SKILL.md contains Scenario 9 for Gantt charts
- Example code matches actual API
- Scenario 4 references Gantt support
</verification>

<success_criteria>
1. Gantt chart examples and API documentation are available in SKILL.md - CHART-02
2. Documentation is consistent with actual implementation
3. Users can understand how to create Gantt charts from the documentation
</success_criteria>

<output>
After completion, create `.planning/phases/05-gantt-chart-api/05-02-SUMMARY.md`
</output>
