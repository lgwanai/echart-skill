---
phase: 13-polling-auto-refresh
plan: 01
type: execute
wave: 1
depends_on: ["12-01", "12-02"]
files_modified:
  - scripts/polling_manager.py
  - requirements.txt
autonomous: true
requirements:
  - POLL-01
  - POLL-02
  - POLL-03
  - POLL-05
---

<objective>
Implement polling infrastructure for automatic data refresh from HTTP sources and database connections using APScheduler.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create polling manager with APScheduler</name>
  <action>
Create scripts/polling_manager.py with:
- PollingConfig: Pydantic model for polling configuration
- PollingManager: APScheduler-based polling orchestrator
- Support for HTTP sources and database connections
- DuckDB table updates on poll
- Last refresh timestamp tracking
  </action>
</task>

<task type="auto">
  <name>Task 2: Add APScheduler dependency</name>
  <action>
Add to requirements.txt:
- apscheduler>=3.10.0

Create the polling infrastructure that:
- Runs in background thread
- Supports interval-based polling
- Updates DuckDB tables on refresh
- Tracks polling metadata
  </action>
</task>

</tasks>

<success_criteria>
- APScheduler integration
- HTTP source polling
- Database connection polling
- DuckDB table updates
- Last refresh tracking
</success_criteria>
