---
phase: 12-http-enhancements
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - scripts/url_data_source.py
autonomous: true
requirements:
  - HTTP-04
  - HTTP-05
  - HTTP-06
---

<objective>
Add POST, PUT, DELETE HTTP methods to URLDataSource for API interactions beyond simple GET requests.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add HTTP method configuration</name>
  <action>
Extend URLDataSourceConfig to support HTTP method and request body.

Add fields:
- method: Literal["GET", "POST", "PUT", "DELETE"] = "GET"
- body: Optional[dict] for JSON request body
- body_type: Literal["json", "form"] = "json"

Update fetch() to use appropriate HTTP method.
  </action>
</task>

<task type="auto">
  <name>Task 2: Implement request methods</name>
  <action>
Add dedicated methods for each HTTP verb:
- fetch_post(): POST with JSON body
- fetch_put(): PUT with JSON body
- fetch_delete(): DELETE request

Update fetch_and_parse() to work with all methods.
  </action>
</task>

</tasks>

<success_criteria>
- GET, POST, PUT, DELETE methods supported
- JSON body support for POST/PUT
- Form data support for POST
- Tests for all methods
</success_criteria>
