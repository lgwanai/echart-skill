---
phase: 07-sqllite-duckdb-sqllite-duckdb
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - scripts/data_merger.py
  - scripts/data_cleaner.py
  - scripts/dashboard_schema.py
  - scripts/url_data_source.py
  - SKILL.md
  - README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "No script imports sqlite3 directly"
    - "All sqlite_master queries replaced with DuckDB equivalents"
    - "conn.cursor() pattern removed from all scripts"
    - "SKILL.md references DuckDB instead of SQLite"
    - "README.md references DuckDB instead of SQLite"
  artifacts:
    - path: "scripts/data_merger.py"
      provides: "DuckDB-compatible data merger"
      exports: ["DataMerger", "MergeConfig"]
    - path: "scripts/data_cleaner.py"
      provides: "DuckDB-compatible data cleaner"
      exports: ["clean_old_data"]
    - path: "scripts/dashboard_schema.py"
      provides: "Dashboard schema (no sqlite_master refs)"
      exports: []
    - path: "scripts/url_data_source.py"
      provides: "URL data source (no sqlite3 refs)"
      exports: ["URLDataSource", "URLDataSourceConfig"]
    - path: "SKILL.md"
      provides: "Updated skill documentation"
      contains: "DuckDB"
    - path: "README.md"
      provides: "Updated project documentation"
      contains: "DuckDB"
  key_links:
    - from: "scripts/data_merger.py"
      to: "database.py"
      via: "get_repository(db_path)"
      pattern: "get_repository\\("
    - from: "scripts/data_cleaner.py"
      to: "database.py"
      via: "get_repository(db_path)"
      pattern: "get_repository\\("
    - from: "SKILL.md"
      to: "scripts/*.py"
      via: "CLI command examples"
      pattern: "workspace\\.duckdb|duckdb"
---

<objective>
Migrate remaining scripts and update documentation to complete the SQLite → DuckDB transition.

Purpose: Close all remaining SQLite references across the codebase and update user-facing documentation. Scripts that only use DatabaseRepository need minimal changes; scripts with direct sqlite3/sqlite_master usage need targeted updates.
Output: All scripts DuckDB-compatible, documentation updated
</objective>

<execution_context>
@/Users/wuliang/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-sqllite-duckdb-sqllite-duckdb/07-RESEARCH.md
@.planning/phases/07-sqllite-duckdb-sqllite-duckdb/07-01-SUMMARY.md
@.planning/phases/07-sqllite-duckdb-sqllite-duckdb/07-02-SUMMARY.md
@scripts/data_merger.py
@scripts/data_cleaner.py
@scripts/dashboard_schema.py
@scripts/url_data_source.py
@SKILL.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate data_merger.py to DuckDB</name>
  <files>scripts/data_merger.py</files>
  <action>
    Migrate data_merger.py from SQLite to DuckDB:

    1. Remove `import sqlite3` (line 8) — not actually used directly, just imported
    2. Update `MergeConfig.db_path` default from `"workspace.db"` to `"workspace.duckdb"`
    3. Update `DataMerger.validate_source_tables()`:
       - Replace `cursor = conn.cursor()` with direct `conn.execute()`
       - Replace `sqlite_master` query with DuckDB equivalent:
         `SELECT table_name FROM information_schema.tables WHERE table_schema = 'main'`
         OR use `conn.execute("SHOW TABLES").fetchall()`
       - `cursor.fetchall()` → `conn.execute(...).fetchall()`
    4. Update `DataMerger.merge_tables()`:
       - `pd.read_sql_query(f"SELECT * FROM {table}", conn)` — this works with DuckDB connection
    5. Update `DataMerger.save_to_database()`:
       - `df.to_sql(self.config.target_table, conn, index=False, if_exists='replace')` — works with DuckDB
    6. Update CLI help text: "合并多个 DuckDB 表格"
    7. Update CLI `--db` default from `"workspace.db"` to `"workspace.duckdb"`
    8. Update class docstring from "SQLite" to "DuckDB"
  </action>
  <verify>
    python -c "from scripts.data_merger import DataMerger, MergeConfig; print('OK')"
    grep -c "sqlite" scripts/data_merger.py | grep -q "^0$" && echo "CLEAN" || echo "STILL HAS SQLITE REFS"
  </verify>
  <done>
    - No sqlite3 import
    - sqlite_master query replaced with information_schema or SHOW TABLES
    - conn.cursor() removed
    - Default db_path is workspace.duckdb
    - CLI help text references DuckDB
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate data_cleaner.py to DuckDB</name>
  <files>scripts/data_cleaner.py</files>
  <action>
    Migrate data_cleaner.py from SQLite to DuckDB:

    1. Update `clean_old_data()`:
       - Replace `cursor = conn.cursor()` with direct `conn.execute()`
       - Replace `sqlite_master` query:
         `SELECT name FROM sqlite_master WHERE type='table' AND name='_data_skill_meta'`
         → DuckDB: Check if table exists via `SELECT table_name FROM information_schema.tables WHERE table_name = '_data_skill_meta'`
         OR simpler: `try: conn.execute("SELECT 1 FROM _data_skill_meta LIMIT 1"); exists = True except: exists = False`
       - Replace `cursor.fetchone()` → `conn.execute(...).fetchone()`
       - Replace `cursor.fetchall()` → `conn.execute(...).fetchall()`
       - Replace `cursor.execute(f"DROP TABLE IF EXISTS {validated_name}")` → `conn.execute(f"DROP TABLE IF EXISTS {validated_name}")`
       - Replace `cursor.execute("DELETE FROM _data_skill_meta WHERE table_name = ?", (table_name,))` → `conn.execute("DELETE FROM _data_skill_meta WHERE table_name = ?", [table_name])`
         Note: DuckDB uses `?` for positional params same as SQLite
    2. Update CLI help text: "Clean up old unused data from DuckDB"
    3. Update CLI `--db` default from `"workspace.db"` to `"workspace.duckdb"`
  </action>
  <verify>
    python -c "from scripts.data_cleaner import clean_old_data; print('OK')"
    grep -c "sqlite" scripts/data_cleaner.py | grep -q "^0$" && echo "CLEAN" || echo "STILL HAS SQLITE REFS"
  </verify>
  <done>
    - No sqlite_master references
    - conn.cursor() removed
    - Default db_path is workspace.duckdb
    - CLI help text references DuckDB
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate dashboard_schema.py and url_data_source.py</name>
  <files>
    - scripts/dashboard_schema.py
    - scripts/url_data_source.py
  </files>
  <action>
    Check and update dashboard_schema.py and url_data_source.py:

    1. **dashboard_schema.py**: Search for any sqlite3 or sqlite_master references.
       - If found: replace with DuckDB equivalents (information_schema or SHOW TABLES)
       - If only uses DatabaseRepository: no changes needed (already works via Plan 01)
       - Update any "SQLite" mentions in docstrings/comments to "DuckDB"

    2. **url_data_source.py**: Search for any sqlite3 or sqlite_master references.
       - If found: replace with DuckDB equivalents
       - If only uses DatabaseRepository: no changes needed
       - Update any "SQLite" mentions in docstrings/comments to "DuckDB"
       - Check for any `pd.to_sql()` calls — these work with DuckDB connection

    For both files: grep for `sqlite`, `sqlite3`, `sqlite_master`, `PRAGMA`, `cursor()` to find any remaining references.
  </action>
  <verify>
    grep -ri "sqlite" scripts/dashboard_schema.py scripts/url_data_source.py 2>/dev/null | grep -v "^Binary" | wc -l
  </verify>
  <done>
    - No sqlite3 imports in either file
    - No sqlite_master or PRAGMA references
    - Docstrings reference DuckDB
  </done>
</task>

<task type="auto">
  <name>Task 4: Update SKILL.md documentation</name>
  <files>SKILL.md</files>
  <action>
    Update SKILL.md to replace all SQLite references with DuckDB:

    1. Replace all mentions of "SQLite" with "DuckDB" in text
    2. Replace `workspace.db` with `workspace.duckdb` in all command examples
    3. Replace `sqlite3` CLI commands with DuckDB equivalents:
       - `sqlite3 workspace.db "PRAGMA table_info(table_name);"` → `python -c "import duckdb; conn = duckdb.connect('workspace.duckdb'); print(conn.execute('DESCRIBE table_name').fetchall())"`
       - `sqlite3 workspace.db "SELECT * FROM table_name LIMIT 3;" -header -column` → `python -c "import duckdb; conn = duckdb.connect('workspace.duckdb'); print(conn.execute('SELECT * FROM table_name LIMIT 3').fetchdf())"`
    4. Update the "核心工作流" section:
       - "导入拦截" — mention DuckDB instead of SQLite
       - "本地探查" — replace sqlite3 examples with DuckDB Python API
       - "非破坏性操作" — DuckDB supports same CREATE TABLE AS pattern
    5. Update all scenario examples that use sqlite3 CLI commands
    6. Update the installation section if it mentions SQLite
    7. Update FAQ section — replace SQLite references
    8. Update the feature table (功能清单) — data_importer description should mention DuckDB

    Key DuckDB CLI equivalents to use in examples:
    - Table info: `python -c "import duckdb; conn = duckdb.connect('workspace.duckdb'); print(conn.execute('DESCRIBE table_name').fetchdf())"`
    - Query: `python -c "import duckdb; conn = duckdb.connect('workspace.duckdb'); print(conn.execute('SELECT ...').fetchdf())"`
    - Or use the DuckDB CLI: `duckdb workspace.duckdb -c "DESCRIBE table_name"`
  </action>
  <verify>
    grep -ci "sqlite" SKILL.md
    grep -c "DuckDB\|duckdb" SKILL.md
  </verify>
  <done>
    - No "SQLite" or "sqlite3" references remain in SKILL.md
    - All command examples use DuckDB
    - workspace.duckdb used as default database path
    - DuckDB advantages mentioned (columnar, analytical performance)
  </done>
</task>

<task type="auto">
  <name>Task 5: Update README.md documentation</name>
  <files>README.md</files>
  <action>
    Update README.md to replace all SQLite references with DuckDB:

    1. Replace "SQLite" with "DuckDB" in all text
    2. Replace `workspace.db` with `workspace.duckdb` in examples
    3. Replace sqlite3 CLI command examples with DuckDB Python API equivalents
    4. Update the "为什么要这个 Skill?" section — mention DuckDB advantages:
       - Columnar storage for analytical queries
       - Better performance for large datasets
       - Direct file reading (CSV, Parquet, JSON)
    5. Update all scenario examples (场景 1-11) that reference sqlite3
    6. Update the core workflow section
    7. Update FAQ section
    8. Update the feature table (功能清单)
    9. Update the "支持连接外部的 MySQL 或 PostgreSQL 吗？" FAQ answer
    10. Keep the privacy message — DuckDB is also local-only by default

    Be thorough — grep for every occurrence of: sqlite, SQLite, sqlite3, workspace.db
  </action>
  <verify>
    grep -ci "sqlite" README.md
    grep -c "DuckDB\|duckdb" README.md
  </verify>
  <done>
    - No "SQLite" or "sqlite3" references remain in README.md
    - All examples use DuckDB
    - DuckDB advantages highlighted in the value proposition
    - workspace.duckdb used as default
  </done>
</task>

</tasks>

<verification>
- grep -ri "import sqlite3" scripts/ — should return nothing
- grep -ri "sqlite_master" scripts/ — should return nothing
- grep -ri "PRAGMA" scripts/ — should return nothing
- grep -ci "sqlite" SKILL.md README.md — should be 0 or only in historical context
- python -c "from scripts.data_merger import DataMerger; from scripts.data_cleaner import clean_old_data; print('All imports OK')"
</verification>

<success_criteria>
- Zero sqlite3 imports across all scripts
- Zero sqlite_master or PRAGMA references across all scripts
- All default db_path values are workspace.duckdb
- SKILL.md fully updated with DuckDB references and examples
- README.md fully updated with DuckDB references and examples
- All scripts import and function correctly with DuckDB backend
</success_criteria>

<output>
After completion, create `.planning/phases/07-sqllite-duckdb-sqllite-duckdb/07-03-SUMMARY.md`
</output>
