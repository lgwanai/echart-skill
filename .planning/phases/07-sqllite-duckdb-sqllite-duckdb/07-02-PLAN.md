---
phase: 07-sqllite-duckdb-sqllite-duckdb
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - scripts/data_importer.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "data_importer.py imports duckdb instead of sqlite3"
    - "File import writes data to DuckDB database"
    - "Metadata tracking (_data_skill_meta) works in DuckDB"
    - "Streaming Excel import works with DuckDB connection"
    - "URL import writes to DuckDB database"
  artifacts:
    - path: "scripts/data_importer.py"
      provides: "DuckDB-compatible data importer for CSV/Excel/URL"
      min_lines: 200
      exports: ["import_to_sqlite", "import_from_url", "import_from_url_sync", "refresh_url_source_sync", "list_url_sources"]
  key_links:
    - from: "scripts/data_importer.py"
      to: "database.DatabaseRepository"
      via: "repo = DatabaseRepository(db_path)"
      pattern: "DatabaseRepository\\("
    - from: "scripts/data_importer.py"
      to: "DuckDB connection"
      via: "conn.execute() replaces cursor.execute()"
      pattern: "conn\\.execute\\("
---

<objective>
Migrate data_importer.py from SQLite to DuckDB — the most complex script with streaming import, URL import, and metadata tracking.

Purpose: data_importer.py is the primary user-facing data entry point. It must work seamlessly with the new DuckDB backend while preserving all existing functionality (CSV/Excel/URL import, duplicate detection, streaming, metadata tracking).
Output: scripts/data_importer.py with DuckDB compatibility
</objective>

<execution_context>
@/Users/wuliang/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-sqllite-duckdb-sqllite-duckdb/07-RESEARCH.md
@.planning/phases/07-sqllite-duckdb-sqllite-duckdb/07-01-SUMMARY.md
@scripts/data_importer.py
@database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace sqlite3 with duckdb in data_importer.py</name>
  <files>scripts/data_importer.py</files>
  <action>
    Migrate all SQLite-specific code in data_importer.py to DuckDB:

    1. Replace `import sqlite3` with `import duckdb` at top of file
    2. Update `init_meta_table()`:
       - Replace `PRAGMA table_info(_data_skill_meta)` with `DESCRIBE _data_skill_meta` or query `information_schema.columns`
       - Replace `sqlite3.OperationalError` catch with `duckdb.Error` or `Exception`
       - `CREATE TABLE IF NOT EXISTS` works same in DuckDB
       - `ALTER TABLE ADD COLUMN` works same in DuckDB
       - Note: DuckDB connection doesn't have `.cursor()` — use `conn.execute()` directly
    3. Update `check_duplicate_import()`:
       - Replace `conn.cursor()` with direct `conn.execute()`
       - `SELECT ... WHERE` works same
       - Replace `cursor.fetchall()` with `conn.execute(...).fetchall()`
    4. Update `record_import()` and `record_url_import()`:
       - Replace `conn.cursor()` with `conn.execute()`
       - `INSERT OR REPLACE` → DuckDB uses `INSERT OR REPLACE` (same syntax)
    5. Update `get_url_sources()`:
       - Replace `conn.cursor()` with `conn.execute()`
    6. Update `import_excel_streaming()`:
       - Change type hint `conn: sqlite3.Connection` → keep generic or use `duckdb.DuckDBPyConnection`
       - Replace `cursor = conn.cursor()` with direct `conn.execute()` calls
       - Replace `cursor.execute()` with `conn.execute()`
       - `conn.commit()` works in DuckDB
    7. Update `_drop_null_columns()`:
       - Change type hint `cursor: sqlite3.Cursor` → remove or use generic
       - Replace `cursor.execute()` with `conn.execute()`
    8. Update `_insert_chunk()`:
       - Change type hint `cursor: sqlite3.Cursor` → remove or use generic
       - Replace `cursor.execute()` with `conn.execute()`
       - Replace `cursor.executemany()` with `conn.executemany()` (DuckDB supports this)
    9. Update `import_to_sqlite()`:
       - The `DatabaseRepository` is already DuckDB-backed from Plan 01
       - `pd.to_sql()` works with DuckDB via SQLAlchemy or use `conn.register('df', df)` + `conn.execute("CREATE TABLE ... AS SELECT * FROM df")`
       - Replace direct `sqlite3.connect(args.db)` calls in `__main__` block with `duckdb.connect(args.db)`
       - Update cursor usage to direct conn.execute()
    10. Update `import_from_url()`:
        - Uses `DatabaseRepository` — already DuckDB from Plan 01
        - `pd.to_sql()` — same consideration as above
    11. Update `refresh_url_source()`:
        - Uses `DatabaseRepository` — already DuckDB
        - Replace `conn.cursor()` with `conn.execute()`
    12. Update `list_url_sources()`:
        - Uses `DatabaseRepository` — already DuckDB
    13. Update CLI `__main__` block:
        - Replace `conn = sqlite3.connect(args.db)` with `conn = duckdb.connect(args.db)`
        - Replace `cursor = conn.cursor(); cursor.execute(...)` with `conn.execute(...).fetchone()`
        - Update description text from "SQLite" to "DuckDB"

    Key pattern changes throughout:
    - `conn.cursor()` → removed (DuckDB executes directly on connection)
    - `cursor.execute(sql, params)` → `conn.execute(sql, params)`
    - `cursor.fetchall()` → `conn.execute(sql, params).fetchall()`
    - `cursor.fetchone()` → `conn.execute(sql, params).fetchone()`
    - `pd.to_sql(table, conn, ...)` → `conn.register('tmp_df', df); conn.execute(f"CREATE TABLE {table} AS SELECT * FROM tmp_df")` OR keep pd.to_sql if DuckDB SQLAlchemy dialect is available

    IMPORTANT: Keep the function name `import_to_sqlite` for backward compatibility but update its docstring to mention DuckDB. The CLI help text should also be updated.
  </action>
  <verify>
    python -c "from scripts.data_importer import import_to_sqlite; print('import OK')"
    python -c "from scripts.data_importer import import_from_url_sync; print('URL import OK')"
    python scripts/data_importer.py file --help 2>&1 | grep -qi "duckdb" && echo "CLI updated" || echo "CLI needs update"
  </verify>
  <done>
    - No `import sqlite3` in data_importer.py
    - All `conn.cursor()` calls removed
    - PRAGMA table_info replaced with DESCRIBE or information_schema query
    - sqlite_master queries replaced with SHOW TABLES or information_schema
    - pd.to_sql replaced with DuckDB-native approach (register + CREATE TABLE AS)
    - CLI help text references DuckDB
    - All public function signatures preserved
  </done>
</task>

</tasks>

<verification>
- python scripts/data_importer.py file test.csv --db /tmp/test.duckdb works without errors
- Import a small CSV file and verify data is queryable
- Verify _data_skill_meta table is created and populated
</verification>

<success_criteria>
- data_importer.py has zero sqlite3 references
- All database operations use DuckDB API
- Streaming Excel import works with DuckDB
- URL import and refresh work with DuckDB
- CLI commands reference DuckDB in help text
- Public API unchanged (function names, parameters)
</success_criteria>

<output>
After completion, create `.planning/phases/07-sqllite-duckdb-sqllite-duckdb/07-02-SUMMARY.md`
</output>
