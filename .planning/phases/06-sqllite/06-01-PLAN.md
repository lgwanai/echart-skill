---
phase: 06-sqllite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/data_merger.py, tests/test_data_merger.py]
autonomous: true
requirements: [MERGE-01, MERGE-03]
user_setup: []

must_haves:
  truths:
    - "User can specify multiple source tables to merge"
    - "Merged data contains a _source_table column to track origin"
    - "Merged data can be saved to SQLite as a new table"
    - "Duplicate table names in source list are rejected"
    - "Non-existent tables cause clear error message"
  artifacts:
    - path: "scripts/data_merger.py"
      provides: "DataMerger class with merge and save functionality"
      exports: ["MergeConfig", "DataMerger"]
    - path: "tests/test_data_merger.py"
      provides: "Test coverage for merge operations"
      contains: "test_merge_tables"
  key_links:
    - from: "scripts/data_merger.py"
      to: "database.py"
      via: "get_repository"
      pattern: "from database import get_repository"
    - from: "scripts/data_merger.py"
      to: "validators.py"
      via: "validate_table_name"
      pattern: "from validators import validate_table_name"
---

<objective>
Create DataMerger class that can merge multiple SQLite tables into one and save to a new table.

Purpose: Enable users to combine data from multiple tables into a single unified table for analysis.
Output: DataMerger class with merge_tables() and save_to_database() methods, plus test coverage.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-sqllite/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From database.py:
```python
class DatabaseRepository:
    def connection(self) -> Iterator[sqlite3.Connection]: ...
    def execute_query(self, query: str, params: tuple = ()) -> list[dict]: ...

def get_repository(db_path: str = "workspace.db") -> DatabaseRepository: ...
```

From validators.py:
```python
def validate_table_name(table_name: str) -> str: ...
```

From data_importer.py:
```python
def clean_column_names(columns) -> list[str]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create MergeConfig pydantic model and DataMerger class skeleton</name>
  <files>scripts/data_merger.py, tests/test_data_merger.py</files>
  <behavior>
    - Test 1: MergeConfig rejects empty source_tables list
    - Test 2: MergeConfig rejects single table (need at least 2)
    - Test 3: MergeConfig rejects duplicate table names
    - Test 4: MergeConfig validates table names using validate_table_name
    - Test 5: DataMerger initializes with valid config
  </behavior>
  <action>
Create scripts/data_merger.py with:

1. MergeConfig pydantic model:
   - source_tables: list[str] with min_length=2
   - target_table: str (validated with validate_table_name)
   - db_path: str = "workspace.db"
   - field_validator to reject duplicate table names
   - field_validator to validate each table name

2. DataMerger class:
   - __init__(self, config: MergeConfig)
   - Uses get_repository(config.db_path) for database access
   - validate_source_tables() method to check tables exist

3. Test file tests/test_data_merger.py:
   - Test MergeConfig validation (empty, single, duplicates)
   - Test DataMerger initialization
   - Use autouse fixture to reset database singleton

Import patterns from existing code:
- from database import get_repository
- from validators import validate_table_name
- import structlog for logging

Reference data_importer.py for structlog pattern and clean_column_names.
Reference data_exporter.py for validate_table_name usage.
</action>
<verify>
  <automated>pytest tests/test_data_merger.py -x -v</automated>
</verify>
<done>
MergeConfig validates all inputs correctly, DataMerger initializes and validates source tables exist
</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement merge_tables and save_to_database methods</name>
  <files>scripts/data_merger.py, tests/test_data_merger.py</files>
  <behavior>
    - Test 1: merge_tables returns DataFrame with all source table rows
    - Test 2: merged DataFrame has _source_table column
    - Test 3: save_to_database creates table with merged data
    - Test 4: save_to_database uses 'replace' mode (overwrites existing)
    - Test 5: merge handles tables with different column schemas (union of columns)
  </behavior>
  <action>
Add to DataMerger class:

1. merge_tables() method:
   - Read each source table using pd.read_sql_query
   - Add _source_table column to track origin
   - Concatenate all DataFrames with pandas.concat
   - Log progress for each table read
   - Return merged DataFrame

2. save_to_database(df, table_name) method:
   - Use df.to_sql with if_exists='replace'
   - Log completion with row count
   - Return the table name saved

3. Test cases:
   - Create test tables with sample data
   - Test merge produces correct row count
   - Test _source_table column values
   - Test save creates queryable table
   - Test column union when schemas differ

Pattern from data_importer.py:
- Use with repo.connection() as conn context manager
- Use pd.read_sql_query for reading tables
- Use df.to_sql for writing
</action>
<verify>
  <automated>pytest tests/test_data_merger.py -x -v</automated>
</verify>
<done>
DataMerger can merge multiple tables and save result to SQLite, with test coverage >= 80%
</done>
</task>

</tasks>

<verification>
1. All tests pass: `pytest tests/test_data_merger.py -x -v`
2. Coverage >= 80%: `pytest tests/test_data_merger.py --cov=scripts.data_merger --cov-report=term-missing`
3. No SQL injection possible (table names validated)
4. Merged data includes _source_table column
</verification>

<success_criteria>
- [ ] MergeConfig validates source tables (min 2, no duplicates, valid names)
- [ ] DataMerger.merge_tables() returns DataFrame with _source_table column
- [ ] DataMerger.save_to_database() writes to SQLite
- [ ] Test coverage >= 80%
- [ ] All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-sqllite/06-01-SUMMARY.md`
</output>
