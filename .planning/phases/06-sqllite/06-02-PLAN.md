---
phase: 06-sqllite
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [scripts/data_merger.py, tests/test_data_merger.py]
autonomous: true
requirements: [MERGE-02, MERGE-04]
user_setup: []

must_haves:
  truths:
    - "User can export merged data to CSV file"
    - "User can export merged data to Excel file"
    - "User can run merge via CLI command"
    - "CLI shows progress during merge operation"
    - "CLI reports total rows merged"
  artifacts:
    - path: "scripts/data_merger.py"
      provides: "Export functionality and CLI command"
      exports: ["export_merged_data", "main"]
    - path: "tests/test_data_merger.py"
      provides: "Test coverage for export and CLI"
      contains: "test_export"
  key_links:
    - from: "scripts/data_merger.py"
      to: "data_exporter.py"
      via: "export patterns"
      pattern: "df.to_csv|df.to_excel"
    - from: "scripts/data_merger.py"
      to: "data_importer.py"
      via: "argparse pattern"
      pattern: "argparse.ArgumentParser"
---

<objective>
Add export functionality and CLI command for data merge operations.

Purpose: Enable users to export merged data to files and run merge via command line.
Output: Export methods and CLI command with comprehensive help.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-sqllite/06-RESEARCH.md

<interfaces>
<!-- Key patterns from existing codebase. -->

From data_exporter.py:
```python
# Export format detection by extension
ext = os.path.splitext(output_path)[1].lower()
if ext == '.csv':
    df.to_csv(output_path, index=False, encoding='utf-8-sig')
elif ext in ['.xlsx', '.xls']:
    df.to_excel(output_path, index=False)
```

From data_importer.py (argparse pattern):
```python
parser = argparse.ArgumentParser(description="...")
parser.add_argument("--db", default="workspace.db", help="...")
args = parser.parse_args()

if __name__ == "__main__":  # pragma: no cover
    main()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add export_merged_data function</name>
  <files>scripts/data_merger.py, tests/test_data_merger.py</files>
  <behavior>
    - Test 1: export_merged_data creates CSV file with correct content
    - Test 2: export_merged_data creates Excel file with correct content
    - Test 3: export_merged_data raises ValueError for unsupported format
    - Test 4: export_merged_data creates output directory if not exists
    - Test 5: CSV export uses utf-8-sig encoding for Chinese support
  </behavior>
  <action>
Add to scripts/data_merger.py:

1. export_merged_data(df, output_path) function:
   - Detect format from file extension (.csv, .xlsx, .xls)
   - For CSV: df.to_csv(output_path, index=False, encoding='utf-8-sig')
   - For Excel: df.to_excel(output_path, index=False)
   - Create output directory if not exists (os.makedirs)
   - Log export completion with row count
   - Raise ValueError for unsupported formats
   - Return output_path

2. Test cases:
   - Test CSV export creates file
   - Test Excel export creates file
   - Test content matches merged data
   - Test unsupported format error
   - Test directory creation

Pattern from data_exporter.py:
- Use os.path.splitext for extension detection
- Use os.makedirs(dir, exist_ok=True) for directory creation
- Use utf-8-sig for CSV (Chinese character support)
</action>
<verify>
  <automated>pytest tests/test_data_merger.py::test_export -x -v</automated>
</verify>
<done>
Export function supports CSV and Excel with proper encoding, tests pass
</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CLI command with argparse</name>
  <files>scripts/data_merger.py, tests/test_data_merger.py</files>
  <behavior>
    - Test 1: CLI with --tables and --target saves to database
    - Test 2: CLI with --export creates export file
    - Test 3: CLI with invalid tables shows error message
    - Test 4: CLI --help shows usage information
    - Test 5: CLI logs progress during merge
  </behavior>
  <action>
Add to scripts/data_merger.py:

1. main() function for CLI:
   - argparse.ArgumentParser with description
   - Required: --tables (nargs='+') for source tables
   - Required: --target for target table name
   - Optional: --db (default='workspace.db')
   - Optional: --export for export file path

2. CLI workflow:
   - Parse arguments
   - Create MergeConfig from args
   - Create DataMerger instance
   - Print progress: "Merging tables: table1, table2..."
   - Call merge_tables()
   - Print progress: "Merged X rows from N tables"
   - Save to database
   - If --export: export to file
   - Print completion message

3. Test CLI:
   - Use subprocess or argparse parse_args patch
   - Test successful merge flow
   - Test export option
   - Test error handling

Pattern from data_importer.py:
- Use subparsers for multiple commands (optional, since this is single command)
- Wrap in if __name__ == "__main__": main()
- Use logger.info for progress, print for user output
</action>
<verify>
  <automated>pytest tests/test_data_merger.py::test_cli -x -v</automated>
</verify>
<done>
CLI command works with all options, tests pass, coverage >= 80%
</done>
</task>

</tasks>

<verification>
1. All tests pass: `pytest tests/test_data_merger.py -x -v`
2. Coverage >= 80%: `pytest tests/test_data_merger.py --cov=scripts.data_merger --cov-report=term-missing`
3. CLI --help shows usage: `python scripts/data_merger.py --help`
4. Full workflow test: `python scripts/data_merger.py --tables t1 t2 --target merged --export merged.csv`
</verification>

<success_criteria>
- [ ] export_merged_data supports CSV and Excel formats
- [ ] CLI command accepts --tables, --target, --db, --export options
- [ ] CLI shows progress and completion messages
- [ ] Test coverage >= 80%
- [ ] All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-sqllite/06-02-SUMMARY.md`
</output>
