---
phase: 08-excel-excel-markdown-table
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/data_importer.py
  - scripts/data_merger.py
autonomous: true
requirements:
  - META-01
  - META-02

must_haves:
  truths:
    - "Every file import records file_path, row_count, and import_time"
    - "Merge operations record parent → child table relationships"
    - "Metadata table has file_path, row_count, parent_tables columns"
  artifacts:
    - path: "scripts/data_importer.py"
      provides: "Extended record_import with file_path and row_count"
      contains: "file_path"
    - path: "scripts/data_merger.py"
      provides: "Merge relationship tracking via record_merge()"
      contains: "record_merge"
    - path: "workspace.duckdb"
      provides: "_data_skill_meta table with new columns"
      contains: "file_path"
  key_links:
    - from: "scripts/data_importer.py"
      to: "_data_skill_meta"
      via: "record_import() INSERT"
      pattern: "file_path.*INSERT"
    - from: "scripts/data_merger.py"
      to: "_data_skill_meta"
      via: "record_merge() INSERT"
      pattern: "parent_tables"
---

<objective>
Extend _data_skill_meta schema and update import/merge recording to capture complete metadata including file paths, row counts, and table relationships.

Purpose: Foundation for history viewing — without complete metadata, the viewer has nothing to show.
Output: Extended schema, updated record_import(), new record_merge() function
</objective>

<execution_context>
@/Users/wuliang/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

# Current schema (from data_importer.py lines 42-54)
@scripts/data_importer.py
# Current merger (from data_merger.py)
@scripts/data_merger.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend _data_skill_meta schema with file_path, row_count, parent_tables columns</name>
  <files>scripts/data_importer.py</files>
  <action>
    In `init_meta_table()`, add ALTER TABLE statements for three new columns after the existing URL column checks:
    - `file_path TEXT` — absolute path of the imported file (distinct from file_name which is just the basename)
    - `row_count INTEGER` — number of rows imported
    - `parent_tables TEXT` — JSON array of source table names, for tracking merge/statistics-derived tables (e.g., '["sales_jan", "sales_feb"]')

    Add these to the migration list alongside the existing url_columns:
    ```python
    new_columns = [
        ('file_path', 'TEXT'),
        ('row_count', 'INTEGER'),
        ('parent_tables', 'TEXT'),
    ]
    ```

    Use the same pattern as existing: check information_schema.columns, ALTER TABLE if missing.

    Update `record_import()` function signature to accept `file_path` and `row_count` parameters:
    ```python
    def record_import(conn, file_name, table_name, md5_hash, file_path=None, row_count=None):
    ```
    Update the INSERT statement to include these new fields. Keep backward compatibility — if not provided, insert NULL.

    Do NOT change the primary key or existing column types.
  </action>
  <verify>Run `python -c "import duckdb; conn = duckdb.connect('workspace.duckdb'); from scripts.data_importer import init_meta_table; init_meta_table(conn); cols = conn.execute('SELECT column_name FROM information_schema.columns WHERE table_name = \'_data_skill_meta\'').fetchall(); print([c[0] for c in cols])"` — output must include file_path, row_count, parent_tables</verify>
  <done>_data_skill_meta table has file_path, row_count, parent_tables columns; record_import() accepts and stores file_path and row_count</done>
</task>

<task type="auto">
  <name>Task 2: Update all record_import() call sites to pass file_path and row_count</name>
  <files>scripts/data_importer.py</files>
  <action>
    Find every call to `record_import()` in data_importer.py and update to pass the new parameters:

    1. CSV import (around line 399): `record_import(conn, file_name, target_table, file_md5)` → add `file_path=os.path.abspath(file_path)` and calculate row_count after import using `conn.execute(f"SELECT COUNT(*) FROM {target_table}").fetchone()[0]`

    2. Excel .et import (around line 437): same pattern — pass absolute file_path and row_count

    3. Excel streaming import (around line 468): pass absolute file_path; row_count is `total_rows` which is already tracked in the loop

    4. Numbers import (around line 520): same pattern

    For each call site, compute row_count AFTER the data is written to DuckDB (not before), using `SELECT COUNT(*) FROM {table_name}`.

    Use `os.path.abspath(file_path)` to ensure consistent absolute paths.
  </action>
  <verify>Grep for `record_import(` — all calls should include file_path and row_count arguments (or explicitly pass None for URL imports which use record_url_import instead)</verify>
  <done>All file-based record_import() calls pass file_path (absolute) and row_count; URL imports unchanged (they use record_url_import)</done>
</task>

<task type="auto">
  <name>Task 3: Add record_merge() function and integrate into DataMerger</name>
  <files>scripts/data_merger.py</files>
  <action>
    Add a new function `record_merge()` in data_merger.py (or import from data_importer if preferred — keep it in data_merger.py to avoid circular imports):

    ```python
    def record_merge(conn, source_tables: list[str], target_table: str, row_count: int):
        """Record merge operation metadata with parent table relationships."""
        import json
        now = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
        conn.execute('''
            INSERT OR REPLACE INTO _data_skill_meta
            (file_name, table_name, import_time, last_used_time, parent_tables, row_count)
            VALUES (?, ?, ?, ?, ?, ?)
        ''', (
            f"merge: {', '.join(source_tables)}",
            target_table,
            now, now,
            json.dumps(source_tables),
            row_count
        ))
        conn.commit()
    ```

    In `DataMerger.save_to_database()`, after saving the merged table, call `record_merge()` with:
    - source_tables: `self.config.source_tables`
    - target_table: `self.config.target_table`
    - row_count: `len(df)`

    Import `json` and `datetime` at the top of data_merger.py if not already present.
  </action>
  <verify>Run a test merge: `python scripts/data_merger.py --tables table_a table_b --target merged_test --db workspace.duckdb`, then query `_data_skill_meta WHERE table_name = 'merged_test'` — parent_tables should contain '["table_a", "table_b"]'</verify>
  <done>Merge operations record parent_tables as JSON array in _data_skill_meta; file_name shows "merge: table_a, table_b" format</done>
</task>

</tasks>

<verification>
- Schema has all new columns (file_path, row_count, parent_tables)
- New file imports populate file_path and row_count
- Merge operations populate parent_tables with source table names
- Backward compatibility: existing records without new fields still queryable
</verification>

<success_criteria>
- _data_skill_meta table extended with 3 new columns
- record_import() accepts and stores file_path + row_count
- All 4 import call sites (CSV, Excel .et, Excel streaming, Numbers) updated
- record_merge() created and called from DataMerger.save_to_database()
- No regression in existing import functionality
</success_criteria>

<output>
After completion, create `.planning/phases/08-excel-excel-markdown-table/08-01-SUMMARY.md`
</output>
