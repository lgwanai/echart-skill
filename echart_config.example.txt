# echart-skill 配置文件模板
# 复制为 echart_config.txt 后按需修改。
# 使用 KEY=VALUE；支持 # 或 ; 注释。

# 本地 HTTP 服务器配置
server.enabled=false
server.port_range=8100,8200

# 图表输出目录（相对于项目根目录）
output.dir=outputs/html

# 百度地图 AK；优先级低于环境变量 BAIDU_AK
baidu_ak=
